import { StoreSectionComponent } from './store-section.component';

describe('StoreSectionComponent', () => {
  let component: StoreSectionComponent;

  beforeEach(() => {
    component = new StoreSectionComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

import { InstagramComponent } from './instagram.component';

describe('InstagramComponent', () => {
  let component: InstagramComponent;

  beforeEach(() => {
    component = new InstagramComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

import { ContactBannerComponent } from './contact-banner.component';

describe('ContactBannerComponent', () => {
  let component: ContactBannerComponent;

  beforeEach(() => {
    component = new ContactBannerComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

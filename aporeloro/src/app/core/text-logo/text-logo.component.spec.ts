import { TextLogoComponent } from './text-logo.component';

describe('TextLogoComponent', () => {
  let component: TextLogoComponent;

  beforeEach(() => {
    component = new TextLogoComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

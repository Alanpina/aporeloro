import { BreakpointObserverService } from './breakpoint-observer.service';

describe('BreakpointObserverService', () => {
  const service: BreakpointObserverService = new BreakpointObserverService();

  it('should create', () => {
    expect(service).toBeTruthy();
  });
});

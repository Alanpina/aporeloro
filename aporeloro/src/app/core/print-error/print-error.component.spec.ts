import { PrintErrorComponent } from './print-error.component';

describe('PrintErrorComponent', () => {
  let component: PrintErrorComponent;

  beforeEach(() => {
    component = new PrintErrorComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
